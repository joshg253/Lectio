"""Screenshot-refresh tooling.

Regenerates the README/docs screenshots from deterministic, fully-synthetic demo
data — so nothing private (e.g. torrent-tracker feeds) can ever leak into a
committed image. See ``scripts/refresh_screenshots.py`` for the entry point.
"""
